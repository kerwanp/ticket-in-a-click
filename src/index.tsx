import * as React from 'react';
import * as ReactDOM from 'react-dom';
import App from './App';
import Layout from './Layout';

ReactDOM.render(<Layout><App /></Layout>, document.getElementById('app'))